# waywallen build environment (conda-forge only).
name: waywallen
channels:
  - conda-forge
  - nodefaults
dependencies:
  # Qt6 >= 6.10 (UI uses Quick / DBus / Protobuf)
  - qt6-main>=6.10
  - qt6-wayland>=6.10
  - freetype
  - expat

  # Used to build qtgrpc from source (conda-forge has no Qt6 Protobuf package)
  - libprotobuf
  - protobuf

  # Toolchain: clang / clang++ / lld all 22.x (BUILD.md requires >=22)
  # clang-tools provides clang-scan-deps (required by C++20 module compilation)
  # build_appimage.sh adds the native activation wrappers, glibc 2.28 sysroot,
  # and Mesa GBM development package for the host architecture.
  - clang=22
  - clangxx=22
  - clang-tools=22
  - lld=22
  - llvm-tools=22

  - libvulkan-headers
  - libvulkan-loader
  - libgles-devel
  - lz4-c
  - libva-utils
  - dav1d-dev

  # FFmpeg
  - nasm
  - make
  - zlib

  # Build infrastructure
  - cmake>=3.28
  - ninja
  - pkg-config
